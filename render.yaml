services:
  - type: web
    name: django-app
    env: python
    buildCommand: 
      PYTHONPATH=. pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn django_ma.wsgi:application